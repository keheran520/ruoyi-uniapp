# TAC验证码最终检查清单

## ✅ 已修复的所有路径

### 1. behaviorCaptcha.ts (工具类)
```typescript
// ✅ 第74行 - 加载TAC SDK
script.src = '/static/load.min.js';

// ✅ 第223行 - 初始化TAC
const tac = await window.initTAC('/static/tac', config, style);
```

### 2. pages/common/captcha/index.vue (webview容器)
```javascript
// ✅ H5平台
captchaUrl.value = `/static/captcha.html?...`;

// ✅ 小程序/APP平台
captchaUrl.value = `${baseUrl}/static/captcha.html?...`;
```

### 3. static/captcha.html (验证码页面)
```javascript
// ✅ 动态获取路径
const currentPath = window.location.pathname;
const basePath = currentPath.substring(0, currentPath.lastIndexOf('/'));
const tacPath = basePath + '/tac';
const tac = await window.initTAC(tacPath, config, style);
```

## 📁 文件位置确认

### 必需文件 (全部在 src/static/)
```
✅ src/static/load.min.js
✅ src/static/captcha.html
✅ src/static/tac/css/tac.css
✅ src/static/tac/js/tac.min.js
✅ src/static/tac/images/icon.png
✅ src/static/tac/images/loading.gif
```

### 验证命令
```powershell
# 检查所有必需文件
Test-Path "C:\Users\TX\Desktop\CZ-RUOYI\ruo-yi-uni-app-plus\src\static\load.min.js"
Test-Path "C:\Users\TX\Desktop\CZ-RUOYI\ruo-yi-uni-app-plus\src\static\captcha.html"
Test-Path "C:\Users\TX\Desktop\CZ-RUOYI\ruo-yi-uni-app-plus\src\static\tac\css\tac.css"
Test-Path "C:\Users\TX\Desktop\CZ-RUOYI\ruo-yi-uni-app-plus\src\static\tac\js\tac.min.js"
```

## 🔄 重启步骤

### 1. 完全停止服务
```bash
# 在终端按 Ctrl+C 停止
# 或关闭终端窗口
```

### 2. 清除缓存（可选但推荐）
```bash
# 删除dist目录
Remove-Item -Path "C:\Users\TX\Desktop\CZ-RUOYI\ruo-yi-uni-app-plus\dist" -Recurse -Force

# 或者
npm run clean  # 如果有这个命令
```

### 3. 重新启动
```bash
npm run dev:h5
```

### 4. 清除浏览器缓存
- 按 F12 打开开发者工具
- 按住 Shift 键点击刷新按钮
- 或者按 Ctrl+Shift+R 强制刷新

## 🎯 预期结果

### 浏览器控制台 - Network标签
应该看到这些请求成功：
```
✅ GET http://localhost:3002/static/load.min.js          200 OK
✅ GET http://localhost:3002/static/tac/css/tac.css      200 OK
✅ GET http://localhost:3002/static/tac/js/tac.min.js    200 OK
✅ GET http://localhost:3002/static/tac/images/icon.png  200 OK
```

### 浏览器控制台 - Console标签
应该看到：
```
✅ 验证码页面URL: /static/captcha.html?...
✅ (没有错误信息)
```

不应该再看到：
```
❌ GET http://localhost:3002/load.min.js 500
❌ 初始化TAC失败
```

## 🧪 测试流程

1. **打开登录页**
   ```
   http://localhost:3002/
   点击登录或直接进入登录页
   ```

2. **选择邮箱登录或号码登录**
   ```
   点击切换到"邮箱登录"或"号码登录"
   ```

3. **输入信息**
   ```
   邮箱: test@example.com
   或
   手机号: 13800138000
   ```

4. **点击获取验证码**
   ```
   应该弹出TAC滑动验证码
   ```

5. **完成验证**
   ```
   滑动拼图到正确位置
   验证成功后自动关闭
   显示"验证码已发送"消息
   ```

## ⚠️ 如果仍有问题

### 检查1: 文件内容是否正确
```powershell
# 查看load.min.js的前几行
Get-Content "C:\Users\TX\Desktop\CZ-RUOYI\ruo-yi-uni-app-plus\src\static\load.min.js" -First 1
# 应该看到 JavaScript 代码
```

### 检查2: 开发服务器是否重启
```bash
# 确保完全停止后重新启动
npm run dev:h5
```

### 检查3: 端口是否冲突
```bash
# 检查3002端口是否被占用
netstat -ano | findstr :3002
```

### 检查4: 直接访问资源
浏览器地址栏输入：
```
http://localhost:3002/static/load.min.js
```
应该下载或显示JavaScript文件内容

### 检查5: vite.config.js
确认没有额外的配置影响static目录：
```javascript
// 检查是否有publicDir配置冲突
// uni-app默认使用src/static，不需要额外配置
```

## 📝 关键修改总结

| 文件 | 行号 | 旧值 | 新值 |
|------|------|------|------|
| behaviorCaptcha.ts | 74 | `/load.min.js` | `/static/load.min.js` |
| behaviorCaptcha.ts | 223 | `/tac` | `/static/tac` |
| captcha/index.vue | 24 | `/captcha.html` | `/static/captcha.html` |
| captcha/index.vue | 32 | `/captcha.html` | `/static/captcha.html` |

## ✨ 完成标志

当你看到以下情况时，表示修复成功：

1. ✅ 点击"获取验证码"后，TAC验证码正常弹出
2. ✅ 控制台没有404或500错误
3. ✅ Network标签显示所有资源加载成功（200状态）
4. ✅ 滑动验证成功后，能正常发送验证码

---

**最后一步：重启开发服务器！** 🚀
```bash
npm run dev:h5
```
